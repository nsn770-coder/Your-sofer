# הנחיות: הוספת בחירת גופן לדף כיפות לאירועים

## 📋 רקע קצר
נוסף לאתר YourSofer טור בחירת גופן לכיפות (Font Selection). הלקוח יכול עכשיו לבחור גופן מרשימת 12 גופנים שונים, ותמונת הגופן תוצג בממשק.

---

## 🔧 שלב 1: העלאת תמונות הגופנים לקלודינרי

### באתר Cloudinary (dyxzq3ucy):

1. **יוצרים תיקייה חדשה** בשם `yoursofer-fonts`
   - ניווט: Media Library → Create folder → `yoursofer-fonts`

2. **מעלים את תמונות הגופנים** (12 תמונות):
   - `font1.png`, `font2.png`, ... `font12.png`
   - גודל מומלץ: **120x120 עד 200x200 pixels** (תמונות קטנות להציגה ב-UI)
   - פורמט: PNG (כדי שתהיה שקיפות אם נדרש)

3. **בדיקה של ה-URL**:
   ```
   https://res.cloudinary.com/dyxzq3ucy/image/upload/v1/yoursofer-fonts/font1.png
   ```

---

## 📊 שלב 2: עדכון Firestore

### לכל מוצר כיפה שיש לו גופן ספציפי:

1. **פתח את Console של Firebase** → Firestore Database
2. **עבור לקולקשן `products`**
3. **בחר מוצר כיפה** (או תיצור שדה חדש לכל המוצרים)
4. **הוסף שדה בשם `fontStyle`** עם ערך כמו:
   - `"font1"` (עבור גופן 1)
   - `"font2"` (עבור גופן 2)
   - וכו'

**דוגמה:**
```
Doc ID: kippa-001
{
  name: "כיפה מקטיפה בלבן",
  price: 45,
  fontStyle: "font3",    ← שדה חדש זה
  subCategory: "כיפות מיוחדות",
  ...
}
```

---

## ✅ שלב 3: אימות בקודם

הקוד הוא כבר מעודכן ב-`KippotEventClient.tsx`:

### שינויים שנוספו:
1. ✅ **Constants** - הוסיפו `FONT_OPTIONS` עם 12 גופנים
2. ✅ **Interface** - הוסיפו `fontStyle?: string;` ל-`Product`
3. ✅ **State** - `const [fontFilter, setFontFilter] = useState('')`
4. ✅ **Filter Logic** - מסנן לפי `p.fontStyle === fontFilter`
5. ✅ **UI** - שורה חדשה עם כפתורי גופנים עם תמונות

---

## 🎨 איפה בעמוד הופיע בחירת הגופן?

**בדף כיפות לאירועים** (`/bar-mitzvah-kippot`):

```
┌─────────────────────────────────┐
│  בחר סגנון   │  בחר חומר  │...  │  ← שורה 1 (קיימת)
├─────────────────────────────────┤
│  בחר גופן: [הכל] [🖼] [🖼] [🖼] │  ← שורה 2 (חדשה - תמונות גופנים)
└─────────────────────────────────┘
```

---

## 🔄 תהליך העבודה של הלקוח

1. **לקוח נכנס לדף** `/bar-mitzvah-kippot`
2. **בוחר סגנון** (הכל / כיפות מיוחדות / כיפות סרוגות)
3. **בוחר חומר** (בד / זמש / משי / וכו')
4. **בוחר גופן** ← כפתור עם תמונת הגופן (חדש!)
5. **רואה רשימה סוננת** של כיפות שמתאימות לכל הבחירות
6. **לוחץ על כיפה** ועובר לדף הפרט למסדרה

---

## ⚙️ התאמות בעתיד

אם תרצה להוסיף עוד גופנים (למשל `font13`, `font14`):

1. **עדכן את `FONT_OPTIONS`** ב-`KippotEventClient.tsx`:
   ```tsx
   const FONT_OPTIONS = [
     { id: 'font1', label: 'גופן 1', url: '...' },
     // ... 
     { id: 'font13', label: 'גופן 13', url: 'https://res.cloudinary.com/dyxzq3ucy/image/upload/v1/yoursofer-fonts/font13.png' },
   ];
   ```

2. **העלה את התמונה** לקלודינרי ב-`yoursofer-fonts`

3. **עדכן את מוצרי הכיפות** בFiestore (הוסף `fontStyle: "font13"` למוצרים החדשים)

---

## 🚀 תיקיית הבדיקה

לאחר העדכון, בדוק:
- [ ] כפתור "בחר גופן" נראה בעמוד
- [ ] תמונות הגופנים מוצגות בצורה נכונה
- [ ] בחירת גופן מסננת את הכיפות בצורה נכונה
- [ ] בחירה על גופן משנה את הצבע של הכפתור
- [ ] כפתור "נקה" מנקה גם את בחירת הגופן

---

## 📝 הערות טכניות

- הקוד כבר עשור לתמונות שאינן בתיקייה — אם הן במקום אחר, רק עדכן את ה-URLs ב-`FONT_OPTIONS`
- כפתורי הגופנים מציגים תמונות בגודל 48x48 (יכול לשנות במידת הצורך)
- ה-`fontStyle` הוא שדה רגיל בFiestore — אפשר להוסיפו בעדכון מוכן (bulk) או בודדות

# ✅ בחירת גופן לכיפות — סיכום סופי

## 📋 מה בנינו

**דף `/bar-mitzvah-kippot` עכשיו כולל בחירת גופן** — עם 13 גופנים שונים שמוצגים ככפתורים עם תמונות.

---

## 🎯 צעדים סיום

### 1️⃣ **עבור כיפות קיימות בFirestore**

אם יש לך כיפות קיימות שצריך לעדכן עם `fontStyle`:

**אפשרות א: Script אוטומטי**
```bash
cd /sessions/lucid-exciting-allen/mnt/your-sofer
npm install firebase-admin (אם לא מותקן)
node scripts/add-fontStyle-to-kippot.js
```

**אפשרות ב: עדכון ידני**
1. פתח Firebase Console → Firestore
2. לכל מוצר כיפה, הוסף שדה `fontStyle` עם ערך בין `"font1"` עד `"font13"`

---

### 2️⃣ **עבור כיפות חדשות**

כשתוסיף כיפה חדשה דרך Admin Dashboard (`/admin/new-product`):

1. בחר קטגוריה: **"כיפות"**
2. בצע את כל הפרטים הנורמליים
3. **יופיע בטופס** ➜ **"§ בחירת גופן"** ← בחר גופן מהרשימה
4. שמור

---

## 📝 מה עודכן בקוד

| קובץ | שינוי | מטרה |
|------|-------|------|
| `KippotEventClient.tsx` | ✅ הוסף `FONT_OPTIONS` עם 13 גופנים + URLs מקלודינרי | UI + filter |
| `KippotEventClient.tsx` | ✅ הוסף state `fontFilter` | מעקב בחירה |
| `KippotEventClient.tsx` | ✅ הוסף filter logic ל-`filtered` | סינון תוצאות |
| `KippotEventClient.tsx` | ✅ הוסף שורת UI עם כפתורי גופנים | ממשק לבחירה |
| `new-product/page.tsx` | ✅ הוסף state `fontStyle` | שמירת בחירה |
| `new-product/page.tsx` | ✅ הוסף select עם 13 גופנים (תנאי: `cat === 'כיפות'`) | UI בטופס |
| `add-fontStyle-to-kippot.js` | ✅ script עדכון batch | עדכון קיימים |

---

## 🎨 איך זה נראה בחזית

### בדף `/bar-mitzvah-kippot`:

```
┌──────────────────────────────────────────────────────────┐
│  בחר סגנון: [הכל] [כיפות מיוחדות] [כיפות סרוגות]          │
├──────────────────────────────────────────────────────────┤
│  חומר: [כל ▼]  |  ₪ מינ'-מקס'  |  מיין: הכי נמכר ▼  [נקה] │
├──────────────────────────────────────────────────────────┤
│  בחר גופן: [הכל] [🖼] [🖼] [🖼] ... [🖼]                  │
│                (תמונות בגודל 48x48)                      │
└──────────────────────────────────────────────────────────┘
```

### בטופס Admin (`/admin/new-product`):

```
§ בחירת גופן
[בחר גופן ▼]
├── גופן 1
├── גופן 2
├── ...
└── גופן 13
```

---

## 🔗 URLs של הגופנים (מקלודינרי)

כל תמונת גופן מתחבר ל-Cloudinary:
- בדוגמה: `https://res.cloudinary.com/dyxzq3ucy/image/upload/v1785227543/1_l7bigs.png`

---

## ✨ טיפים

### אם תרצה להוסיף גופנים נוספים:

1. **בקלודינרי:** העלה תמונות חדשות (תתן להם שמות)
2. **בקוד:** הוסף למערך `FONT_OPTIONS` ב-`KippotEventClient.tsx`:
   ```tsx
   { id: 'font14', label: 'גופן 14', url: 'https://res.cloudinary.com/dyxzq3ucy/image/upload/v.../14_xxxxx.png' },
   ```
3. **בAdmin:** הוסף `<option value="font14">גופן 14</option>`

### אם תמונות הגופן לא נטענות:

- בדוק ש-URLs בקלודינרי פעילים
- בדוק את console.log בdeveloper tools (F12 → Network)

---

## 🚀 בדיקה סופית

לפני העלאה לייצור, תרצה:
- [ ] דף כיפות מוצג כפתורי גופנים
- [ ] בחירת גופן מסננת כיפות נכון
- [ ] Admin Dashboard מציג בחירת גופן לכיפות חדשות
- [ ] כיפות קיימות יש להם `fontStyle` בFirestore (אם יש)

---

## 📞 שאלות?

אם משהו לא עובד:
1. בדוק את הקונסול (F12)
2. בדוק שכל ה-URLs של תמונות הגופן פעילים
3. וודא ש-`fontStyle` קיים בFirestore עבור כיפות שרוצה לסנן

**Done! 🎉**
